/*for (let i = 0; i <= 5; i++) {
    const evenOrOdd = i % 2 === 0 ? "par" : "ímpar";
    console.log(`${i} é ${evenOrOdd}.`);
}*/

const fruits = ["Apple", "Pear", "Grape"]

for (let i = 0; i < fruits.length; i++) {
    const element = fruits[i];
    console.log(element);
}

